# React Lifecycle - ASCII Diagrams

## 🏗️ Mounting Lifecycle

```
┌─────────────────┐
│   constructor() │ ← Initialize state, bind methods
└─────────────────┘
         ↓
┌─────────────────┐
│    render()     │ ← Create Virtual DOM, NO side effects
└─────────────────┘
         ↓
┌─────────────────┐
│ componentDidMount() │ ← API calls, DOM manipulation, subscriptions
└───────────────────┘
```

**Functional Component Equivalent:**
```jsx
useEffect(() => {
  // componentDidMount
  apiCall();
  return () => {
    // componentWillUnmount
    cleanup();
  };
}, []); // ← Empty dependency array
```

## 🔄 Update Lifecycle

### Class Component Update Flow:
```
┌─────────────────────┐
│ shouldComponentUpdate│ ← Return true/false to optimize re-renders
│  (nextProps, nextState) │
└─────────────────────┘
         ↓
┌─────────────────┐
│    render()     │ ← Re-render with new props/state
└─────────────────┘
         ↓
┌─────────────────┐
│ componentDidUpdate │ ← DOM operations after update
│ (prevProps, prevState) │
└─────────────────┘
```

### Functional Component Update Flow:
```
┌─────────────────┐
│   State/Props   │ ← Change detected
│     Change      │
└─────────────────┘
         ↓
┌─────────────────┐
│  Component      │ ← Re-render function
│   Re-render     │
└─────────────────┘
         ↓
┌─────────────────┐
│   useEffect     │ ← Runs if dependencies changed
│  (with deps)    │
└─────────────────┘
```

## 🗑️ Unmounting Lifecycle

```
┌─────────────────┐
│ componentWill   │ ← Cleanup timers, subscriptions,
│  Unmount()      │   cancel API requests
└─────────────────┘
```

## ⚛️ Complete Lifecycle Flow

```
MOUNTING:
constructor() → render() → componentDidMount()

UPDATING:
New Props/State → shouldComponentUpdate() → render() → componentDidUpdate()

UNMOUNTING:
componentWillUnmount()
```

## 🎯 Hooks Lifecycle Mapping

```
Class Components:          Functional Components:
─────────────────────────────────────────────────
constructor()           ← useState() initialization
render()                ← Function body execution
componentDidMount()     ← useEffect(() => {}, [])
componentDidUpdate()    ← useEffect(() => {})
componentWillUnmount()  ← useEffect return function
shouldComponentUpdate() ← React.memo() / useMemo()
```

## 🔄 Detailed Update Cycle with Virtual DOM

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   State/Props   │ →  │  Virtual DOM    │ →  │   Diffing &     │
│     Change      │    │   Re-render     │    │ Reconciliation  │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                ↓
┌─────────────────┐    ┌─────────────────┐
│  Commit Phase   │ →  │  Real DOM       │
│   (React DOM)   │    │   Updates       │
└─────────────────┘    └─────────────────┘
```

## 💡 Key Points

- **Mounting**: Component created and inserted into DOM
- **Updating**: Component re-renders due to state/props changes  
- **Unmounting**: Component removed from DOM
- **Virtual DOM**: Makes the process efficient by minimizing actual DOM operations

**Remember**: Hooks provide the same capabilities with simpler syntax! 🚀
